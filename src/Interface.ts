export interface JSON_FORMAT {
    targetId: string;
    size: number;
    inportData?: Array<Array<number>>;
    scale?: number;
    bgColor?: string;
    color?: string;
    // positionId: string;
}